
# docker run -d -p 27017:27017 --name mongo.catalog -v mongodb:/data/db mongo

version: "3.8"

services:
  mongo:
    image: mongo
    container_name: mondo.catalog
    ports:
      - 27017:27017
    volumes:
      - mongodbdata:/data/db

volumes:
  mongodbdata: